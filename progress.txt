- the fixture is read as text for the parser and dynamically imported for the runtime array, so the two views come from one file
  trigger: when a test must both parse and execute the same config file
  kind: pattern
  cause: parity needs identical source on both sides
  resolution: keep the fixture in src/reader/fixtures/ and use Bun.file(url).text() plus import(url.href)
  signal: silent
- the build now emits dist/reader/read-entries.d.ts, which imports type from @typescript-eslint/types, a devDependency
  trigger: when running bun run build after adding src/reader
  kind: location
  cause: tsconfig.build.json emits declarations for everything under src, not only what index.ts reaches
  resolution: left as is; the exports map exposes only dist/index.*, and check-pack passes
  artifact: dist/reader/read-entries.d.ts
  signal: silent
- 10 deliberate code breaks (computed-key check, unary operator, two-call check, array opacity, dotted-key split, __proto__, type-only import, as-unwrap, element guard, duplicate-key order) each made at least one test fail
  trigger: when a new module passes all its tests on the first green run
  kind: pattern
  cause: a suite that passes at once proves nothing until a broken implementation is shown to fail it
  resolution: apply each break with perl to a backed-up copy, run bun test src/reader, then restore the file and check it with cmp
  signal: silent
- import/order wants all import type lines in the first block, with ./read-entries sorted before @typescript-eslint/types
  trigger: when a test file has both a relative and a package import type
  kind: gotcha
  cause: the type group comes first and is alphabetized, and '.' sorts before '@'
  resolution: put every import type line in one leading block, in the order the lint message gives
  artifact: `./read-entries` type import should occur before type import of `@typescript-eslint/types`
  signal: loud
- toEqual on a mapped array of node.type values against string literals fails check-types, although bun test passes
  trigger: when asserting on an array of node.type values in a test
  kind: gotcha
  cause: the mapped array is AST_NODE_TYPES.ObjectExpression[] and expect's overload rejects the string literal array
  resolution: assert with every((node) => node.type === 'ObjectExpression') instead
  artifact: Type '"ObjectExpression"' is not assignable to type 'AST_NODE_TYPES.ObjectExpression'.
  signal: loud
- string-literal comparison against the AST_NODE_TYPES enum type-checks for real node names and errors for bogus ones, so an import type of TSESTree needs no runtime enum import
  trigger: when comparing TSESTree node.type against a string literal
  kind: pattern
  cause: TS allows comparing a string enum member type with a string literal of the same value
  resolution: use import type from @typescript-eslint/types and compare node.type with string literals; a bogus name is the control
  artifact: This comparison appears to be unintentional because the types 'AST_NODE_TYPES' and '"NotANodeType"' have no overlap.
  signal: loud
- scaffold is complete and functional
  trigger: all gates passed on first run
  kind: pattern
  signal: loud
- a passing check-pack is only meaningful next to a control that plants a stray file and a file: specifier
  trigger: when proving check-pack can fail
  kind: pattern
  cause: a clean manifest exits 0 whether or not the checks work
  resolution: temporarily add tsconfig.json to files and set the dependency to file:../define-config; expect exit 1 with both problems, then restore package.json
  artifact: check-pack: packed tarball holds tsconfig.json, which is outside the allow-list
  signal: silent
- Bun.semver offers satisfies and order but no validRange
  trigger: when validating that a dependency specifier is a semver range under bun
  kind: gotcha
  cause: bun's semver API has no range-validity check
  resolution: a small regex over npm's range grammar (comparators, x-ranges, hyphen ranges, ||) in isSemverRange, with accept and reject tables in its tests
  artifact: Bun.semver
  signal: silent
- requiring README.md and LICENSE in the pack would fail check-pack until the next scaffold task adds them
  trigger: when checking the pack before README.md and LICENSE exist
  kind: gotcha
  cause: the task sets an allow-list, and those files do not exist yet
  resolution: only package.json, dist/index.js and dist/index.d.ts are required; README.md and LICENSE are allowed, not required
  artifact: packed tarball is missing
  signal: silent
- the library's fence settings name its own package, which here is a real runtime dependency
  trigger: when adapting the markdown-fence settings from ../define-config
  kind: gotcha
  cause: the library ignored and pinned its own unbuilt name; in this package that name resolves from node_modules
  resolution: point the fence no-unresolved ignore and import/internal-regex at @open-tomato/eslint-plugin-define-config
  artifact: 'import/internal-regex': '^@open-tomato/define-config$'
  signal: silent
- a clean eslint . cannot prove the ignores work; putting the same bad file in both a linted directory and each ignored one proves both the rules and the ignores
  trigger: when checking that eslint ignores really exclude a directory
  kind: pattern
  cause: an ignored directory holding no bad files gives the same exit 0 as one that is linted
  resolution: put a double-quoted, semicolon-free unused const in src/ and in each ignored directory; only the src/ copy should report
  artifact: Strings must use singlequote
  signal: silent
- bun blocks the unrs-resolver postinstall and reports it as untrusted
  trigger: when adding eslint-import-resolver-typescript with bun add
  kind: gotcha
  cause: bun runs lifecycle scripts only for trusted dependencies, and ../define-config does not trust this one either
  resolution: left untrusted; eslint . works without the script
  artifact: Blocked 1 postinstall. Run `bun pm untrusted` for details.
  signal: loud
- the placeholder entry cannot show whether --packages external works; a throwaway probe importing merge, built with and without the flag, shows it
  trigger: when verifying the build keeps dependencies external while src/index.ts imports nothing
  kind: pattern
  cause: a bundle with no imports looks the same either way
  resolution: check dist/index.js for 'from "@open-tomato/define-config"' once the rules stage imports the library
  artifact: --packages external
  signal: silent
- bun test exits 1 when it discovers zero test files
  trigger: when running bun test before any *.test.ts file exists
  kind: gotcha
  cause: bun test treats no matching files as an error
  resolution: expected until scripts/pack-check.test.ts lands in a later scaffold task
  artifact: error: 0 test files matching **{.test,.spec,_test_,_spec_}.{js,ts,jsx,tsx}
  signal: loud
- on bun 1.3.14 the user ~/.npmrc scope registry wins over bunfig.toml [install.scopes]; only a project-level .npmrc overrides it
  trigger: when relying on bunfig.toml [install.scopes] to override a user-level ~/.npmrc scope route
  kind: gotcha
  cause: bun's registry precedence puts the user ~/.npmrc scope route ahead of the bunfig scope table, whether the key is written with or without the @
  resolution: keep a project .npmrc with @open-tomato:registry=https://registry.npmjs.org/; bunfig.toml alone still fails
  artifact: error: GET https://npm.heimdall.bifemecanico.com/@open-tomato%2fdefine-config - 404
  signal: loud
