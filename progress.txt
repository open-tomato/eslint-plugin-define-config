- The intact copy fails type-checking unless package.json is copied too.
  trigger: when type-checking a scratch copy of src/
  kind: gotcha
  cause: src/index.test.ts imports ../package.json
  resolution: copy package.json into the scratch dir beside src
  artifact: Cannot find module '../package.json'
  signal: loud
- import/order wants the parent group, the sibling group and the ./index import each in its own group, separated by blank lines
  trigger: when an index test imports ../package.json beside sibling and index imports
  kind: pattern
  cause: the index path group is separate from sibling in sharedRules.mjs
  resolution: put ../package.json first, then the ./x imports, then import plugin from './index' after a blank line
  artifact: There should be at least one empty line between import groups
  signal: loud
- a host that also registers the plugin under the same prefix makes ESLint throw
  trigger: when a flat-config preset registers a copy of the plugin rather than the plugin itself
  kind: gotcha
  cause: @eslint/config-array refuses two different objects under one plugin name
  resolution: build the plugin base object, then add configs to it in place (Object.assign) so the preset holds the plugin itself
  artifact: Cannot redefine plugin "define-config"
  signal: loud
- every plugin.configs / plugin.meta access warns under eslint-plugin-import, in this repo and in any host using it
  trigger: when src/index.ts exports named meta/rules/configs alongside a default plugin object
  kind: gotcha
  cause: import/no-named-as-default-member fires when a default export's member shares a name with a named export
  resolution: make the plugin object the only value export; keep meta, rules and configs as its properties
  artifact: also has a named export `configs`. Check if you meant to write `import {configs} from './index'` instead
  signal: loud
- the settings rule reports on read.call.callee, matching where the locator puts findings owned by defaults
  trigger: when choosing the node the settings rule reports on
  kind: pattern
  cause: reporting on the whole call spans the entire config array
  resolution: report on the callee loc (line 3, columns 16-28 in the shared sourceOf fixture)
  artifact: const { loc } = read.call.callee;
  signal: silent
- a backtick string with no interpolation fails @stylistic/quotes even when it contains backticks itself
  trigger: when writing a message constant as a template literal split across concatenated lines
  kind: gotcha
  cause: quotes rule is single with no allowTemplateLiterals, so only interpolating templates are allowed
  resolution: write the piece as a single-quoted string with escaped apostrophe
  artifact: Strings must use singlequote  @stylistic/quotes
  signal: loud
- report with loc: located.node.loc instead of node, so no ESTree/TSESTree cast is needed
  trigger: when a rule reports a TSESTree node through eslint's Rule.RuleContext
  kind: pattern
  cause: eslint types report nodes as ESTree while the locator returns TSESTree nodes
  resolution: only the Program handed to readEntries is cast, with a comment; reports use loc
  artifact: context.report({ loc: located.node.loc
  signal: silent
- flow-shaped arrays end in }] with no trailing comma, so appending an element produced invalid source
  trigger: when appending a non-literal element to build an unsupported file
  kind: gotcha
  cause: the helper assumed every fixture array ended with a comma and newline
  resolution: strip any trailing comma and whitespace, then append ', extra]'
  artifact: A fatal parsing error occurred: Parsing error: ',' expected.
  signal: loud
- a silent case built from another code's config is what proves a rule filters by code; the handler-conflict config also yields unreachable, which checks the reported case's error count
  trigger: when a rule-test fixture's only finding is the rule's own code
  kind: pattern
  cause: a rule that reports every surviving finding passes any test whose config holds only its own code
  resolution: added a silent case built from another code's config to each rule, and checked that removing the filter fails all nine
  artifact: silent on another code's finding
  signal: silent
- the opaque cases for required-dropped, cycle and interactive-unattended pass with the suppression removed
  trigger: when proving each rule's opaque case is silenced by the suppression
  kind: gotcha
  cause: the placeholder string never produces those codes: it does not drop a required key, it becomes an unknown-step rather than a cycle, and it is not true so $unattended is off
  resolution: kept them as silent opaque cases and did not count them as proof of the suppression; the other 6 rules' opaque cases fail without it
  artifact: 62 pass 6 fail
  signal: silent
- defaults are concatenated first, so defaults can never override a file entry
  trigger: when a task asks for a handler set in the file and overridden in defaults
  kind: gotcha
  cause: merge order is defaults then file entries
  resolution: tested the file-and-defaults both set case, which reports on the file line
  signal: silent
- matching a key against every dot-joined run of path keys handles both 'a.b' spanning ['a','b'] and a path key that itself holds a dot
  trigger: when matching a written object key against a diagnostic path
  kind: pattern
  cause: provenance and required paths dot-join keys, so a dotted key and nested keys share a path
  resolution: spanOf finds the n with path.slice(from, from + n).join('.') === key
  artifact: spanOf
  signal: silent
- sed mutations of the locator (no defaults subtraction, no dotted span, counting removes, first match instead of last, no defaults branch) each turn tests red
  trigger: when checking that locator tests can fail
  kind: pattern
  cause: a passing suite alone does not show the assertions constrain the behaviour
  resolution: back up locate.ts, apply one mutation, run bun test src/locate.test.ts, restore, and diff against the backup
  artifact: 41 pass  10 fail <- no subtraction
  signal: silent
- an untyped const array literal infers string[] and fails check-types while bun test passes
  trigger: when asserting toEqual on a literal string array against DiagnosticCode values
  kind: gotcha
  cause: expect(DiagnosticCode[]).toEqual narrows its parameter to the union
  resolution: annotate the expected array as DiagnosticCode[]
  artifact: error TS2769: No overload matches this call.
  signal: loud
- provenance at flows.f.a names only the entry that started the step map, not a later entry that merged keys into it
  trigger: when locating a graph finding whose path ends at a step id (handler-conflict, interactive-unattended, unreachable)
  kind: gotcha
  cause: merge records nothing at a path for a map merged into an existing map, only at the keys it writes below
  resolution: accepted as the spec's rule; pinned by the test 'ties a graph finding to the entry that started its map, not one that merged into it'
  artifact: a map merged into a map already at the path records nothing at the path itself
  signal: silent
- the tsconfig lib target lacks es2023, so toSorted fails check-types while bun test still passes
  trigger: when using Array.prototype.toSorted in a *.test.ts file
  kind: gotcha
  cause: tsconfig lib is older than es2023
  resolution: copy and sort with [...items].sort()
  artifact: Property 'toSorted' does not exist on type
  signal: loud
- The cache accepts a compute thunk, so tests use stand-in objects as the SourceCode key and Analysis value and never parse real source
  trigger: when caching per linted file
  kind: pattern
  cause: the WeakMap only uses the identity of its key and value
  resolution: cast plain objects to SourceCode and Analysis in the tests
  artifact: cachedAnalysis(sourceCode, compute)
  signal: silent
- six perl breaks of suppress.ts (reachOf identity, one-way overlaps, no per-flow scope, non-flow scope as own path, joined-string prefix, graph unfiltered) each failed between 1 and 7 tests
  trigger: when a suppression test passes on its first run
  kind: pattern
  cause: a suite that passes at once proves nothing until a broken implementation fails it
  resolution: back up the file, apply each break, run bun test on the one file, restore and check with cmp
  signal: silent
- [{ a: 1 }, { $replace: true, a: 2 }] still yields duplicate-key at a, so an opaque root $replace suppresses a finding the loader would report
  trigger: when checking whether a root-level $replace resets duplicate-key tracking
  kind: location
  cause: the library records duplicates per layer and does not clear them on a root replace
  resolution: accepted as under-reporting, which is allowed by design
  signal: silent
- the reader records the opaque path as ['flows','$replace'], which overlaps no ['flows', f], so the plain prefix rule keeps a defaults flow's finding that a literal true would replace away
  trigger: when an opaque value is the $replace key itself, as in flows: { $replace: flag }
  kind: gotcha
  cause: the placeholder is not true, so the merge does not replace, while a true value at runtime would wipe the holding object's merged subtree
  resolution: reachOf maps an opaque path ending in $replace to its parent path before the overlap checks
  artifact: unreachable@flows.g.d
  signal: silent
- artifact: PLAN_TRACKER-rafa-2-eslint-plugin.md: discovers and runs `**/*.test.ts` files in parallel
- PIPESTATUS printed nothing, so the exit code was not read
  trigger: when reading a gate's exit code through a pipe in the Bash tool
  kind: gotcha
  cause: the shell is zsh, whose array is the lowercase pipestatus
  resolution: redirect each gate to a file and read $? directly
  signal: silent
- check-types fails because tsconfig sets lib ES2022; bun test passes anyway
  trigger: when using Array.prototype.toSorted in a test
  kind: gotcha
  cause: toSorted is ES2023
  resolution: use [...array].sort()
  artifact: Property 'toSorted' does not exist on type 'string[]'
  signal: loud
- the published 0.4.0 resolveGraph takes Readonly<Record<string, unknown>>, so no cast is needed once flows is narrowed, unlike the progress.txt note predicted
  trigger: when calling resolveGraph on the merged flows
  kind: location
  cause: the earlier cast was needed only because value['flows'] is unknown
  resolution: narrow with an isRecord check in flowsOf, the way the loader does
  artifact: export declare function resolveGraph(flows: Readonly<Record<string, unknown>>, steps: StepRegistry): ResolveGraphResult;
  signal: silent
- a string flows cannot tell a checked flowsOf from an unchecked one; an array flows can
  trigger: when testing that analyse reads a non-object flows as no flows
  kind: pattern
  cause: resolveGraph finds no plain-object flow in a string's characters but reads an array's item 0 as a flow named '0'
  resolution: the test includes flows: [{ a: { step: 'nope' } }] and expects no findings
  signal: silent
- a placeholder handler target, step name or $start is reported as unknown-step at the opaque path itself, so it never resolves to a real step
  trigger: when checking what the placeholder does inside the graph
  kind: pattern
  cause: a handler target is looked up as a step entry of the flow or a registered step, and the placeholder is neither
  resolution: a PLACEHOLDER test asserts the three unknown-step paths, sorted, since the library's reporting order is internal
  artifact: which is neither a step entry of flow
  signal: silent
- an opaque $replace merges as the placeholder and never replaces, yet its path is neither a prefix of nor has as a prefix the paths it could drop
  trigger: when writing the merge-path prefix rule in src/analysis/suppress.ts
  kind: gotcha
  cause: ['a','$replace'] is not a prefix of ['a','x'], and a top-level ['$replace'] is not under flows at all
  resolution: count an opaque path ending in $replace as its parent path when suppressing
  artifact: opaque.opaque equals [[['a', '$replace']]]
  signal: silent
- a wholly opaque entry (top-level spread or computed key) has the opaque path [], which the per-flow graph rule as worded never matches
  trigger: when writing the suppression rules in src/analysis/suppress.ts
  kind: gotcha
  cause: the rule only checks for opaque paths lying under flows.<f>; the root path [] is a prefix of flows.<f>, not under it
  resolution: treat an opaque path that is a prefix of flows.<f>, [] included, as covering that flow
  artifact: analysis.opaque[i] equals [[]]
  signal: silent
- unknown keys under settings['define-config'] are reported as malformed, so a misspelt 'step' is not a silent graph-rules-off
  trigger: when deciding what counts as malformed settings
  kind: pattern
  cause: the spec's risk list names a settings mistake that silences every rule
  resolution: unknownKeyProblems lists each unknown key after the field problems; remove that line to allow extra keys
  signal: silent
- DuplicatesOption is not exported from the @open-tomato/define-config index
  trigger: when typing the duplicates option in plugin code
  kind: location
  cause: the index re-exports MergeOptions but not merge/duplicates types
  resolution: derive it as NonNullable<MergeOptions['duplicates']>, exported here as DuplicatesSetting
  artifact: export type DuplicatesOption = 'warn' | 'error' | 'allow';
  signal: silent
- resolveGraph's first parameter is the typed Flows record, and MergeResult.value entries are unknown, so tsc refuses the call
  trigger: when passing merge(...).value['flows'] to resolveGraph in a test
  kind: gotcha
  cause: MergeResult.value is Readonly<Record<string, unknown>>
  resolution: cast with 'as Parameters<typeof resolveGraph>[0]'; analyse.ts will need the same narrowing
  artifact: Argument of type 'unknown' is not assignable to parameter of type 'Readonly<Record<string, unknown>>'
  signal: loud
- the fixture is read as text for the parser and dynamically imported for the runtime array, so the two views come from one file
  trigger: when a test must both parse and execute the same config file
  kind: pattern
  cause: parity needs identical source on both sides
  resolution: keep the fixture in src/reader/fixtures/ and use Bun.file(url).text() plus import(url.href)
  signal: silent
- the build now emits dist/reader/read-entries.d.ts, which imports type from @typescript-eslint/types, a devDependency
  trigger: when running bun run build after adding src/reader
  kind: location
  cause: tsconfig.build.json emits declarations for everything under src, not only what index.ts reaches
  resolution: left as is; the exports map exposes only dist/index.*, and check-pack passes
  artifact: dist/reader/read-entries.d.ts
  signal: silent
- 10 deliberate code breaks (computed-key check, unary operator, two-call check, array opacity, dotted-key split, __proto__, type-only import, as-unwrap, element guard, duplicate-key order) each made at least one test fail
  trigger: when a new module passes all its tests on the first green run
  kind: pattern
  cause: a suite that passes at once proves nothing until a broken implementation is shown to fail it
  resolution: apply each break with perl to a backed-up copy, run bun test src/reader, then restore the file and check it with cmp
  signal: silent
- import/order wants all import type lines in the first block, with ./read-entries sorted before @typescript-eslint/types
  trigger: when a test file has both a relative and a package import type
  kind: gotcha
  cause: the type group comes first and is alphabetized, and '.' sorts before '@'
  resolution: put every import type line in one leading block, in the order the lint message gives
  artifact: `./read-entries` type import should occur before type import of `@typescript-eslint/types`
  signal: loud
